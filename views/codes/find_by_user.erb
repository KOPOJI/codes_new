<%#include "codes.h" %>

<% tfetch(QString, error) %>
<% if(error.isEmpty()) { %>
<% tfetch(QList<Codes>, codes) %>
<% tfetch(Users, user) %>
<%= H::tr("Found") %> <%= codes.count() %> <%= H::tr("message(s) by") %> <b><%= user.username() %></b>:<br><br>
    <% for(const auto& code: codes) { %>

<%== linkTo(code.title(), H::createUrl({"codes", QString::number(code.id())})) %><br>
    <% } %>
<% } else { %>
    <%== error %>
<% } %>
